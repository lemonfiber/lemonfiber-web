dist/
coverage/
storybook-static/
node_modules/
src/generated/
src/paraglide/

# Written by inlang, not by hand.
project.inlang/

# Shared verbatim with the other lemonfiber repos. Byte-identity is the point —
# reformatting them here would fork a file that is meant to have one home.
.markdownlint.jsonc
.github/labeler.yml
